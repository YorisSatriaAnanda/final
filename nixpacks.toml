[phases.setup]
nixPkgs = ["...", "php83", "nodejs", "npm"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm ci",
    "npm run build",
]

[start]
cmd = "bash start.sh"
